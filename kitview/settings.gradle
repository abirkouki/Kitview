rootProject.name = 'kitview'

include ':practice', ':orthalis-connect' , ':practice_lib'

rootProject.children.each {
    if (it.name.startsWith('practice')) {
        it.name = rootProject.name + '-' + it.name
    }
}